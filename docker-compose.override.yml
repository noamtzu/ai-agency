services:
  worker:
    environment:
      # Do not override GPU_SERVER_URL here; allow .env / docker-compose.yml defaults to apply.
    depends_on:
      redis:
        condition: service_started

  gpu_server:
    profiles: ["gpu"]

